# The name of the default config class for the app (referenced in apps.py
# where the signals module imported), so django aware of customer ready
# method and signals work.
default_app_config = 'checkout.apps.CheckoutConfig'
